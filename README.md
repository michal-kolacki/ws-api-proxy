# twojtrener.pl

## Preinstallation

- yarn
- `yarn install`
- `npm install -g typescript`

## Running DEV / LOCAL

- copy `env/.env.example` to `env/.env` and fill proper data
- `tsc -w`
- `yarn start`
